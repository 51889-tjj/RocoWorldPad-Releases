# RocoWorldPad 2.1.18

## 贴图显示规则

- 贴图统一从层级右上角的贴图入口管理，不再在识图改键或按键改键中打开不稳定的重复入口。
- 贴图属性可选择是否由当前层的识图改键、按键改键规则控制。
- 开启规则控制后，贴图默认隐藏；规则触发时按设定显示或隐藏。
- 识图改键会跟随模板的实时状态：模板出现时显示，模板消失时自动隐藏。

## 配置与运行时

- 贴图可见性控制会写入配置，并在导入、导出和运行时同步中保留。
- 修复运行时同步遗漏贴图可见性控制字段的问题。
